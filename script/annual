require File.dirname(__FILE__) + '/../lib/community_usage'

Interface.start

Export.make_directory 'annual'
# 
# AnnualCuriosities.new.highest_visits
# puts; puts
# AnnualCuriosities.new.lowest_visits(3)

Export.to_list(AnnualCuriosities.new.growing(10), 'growing-communities')
Export.to_list(AnnualCuriosities.new.shrinking(10), 'shrinking-communities')

Export.to_list(AnnualCuriosities.new.visits(10), 'visits')
Export.to_list(AnnualCuriosities.new.visitors(10), 'visitors')
Export.to_list(AnnualCuriosities.new.pageviews(10), 'pageviews')
Export.to_list(AnnualCuriosities.new.time_on_site(10), 'time_on_site')
Export.to_list(AnnualCuriosities.new.bounces(10), 'bounces')
Export.to_list(AnnualCuriosities.new.bounces(10), 'new_visits')

[:visits, :visitors, :pageviews, :new_visits, :bounces, :time_on_site, :entrances, :exits, :total_events].each do |arg|
